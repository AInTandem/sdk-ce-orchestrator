# Use public npm registry
@aintandem:registry=https://registry.npmjs.org/

# Disable provenance (2FA disabled, using NPM_TOKEN instead)
provenance=false

# Authenticate with npm using NPM_TOKEN environment variable
# pnpm will read this token when publishing packages
//registry.npmjs.org/:_authToken=${NPM_TOKEN}